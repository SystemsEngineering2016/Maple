<body>
<div class = "jumbotron">
  <h1 align="center">Edit this Version</h1>

  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>">
      <a href="#" data-dismiss="alert" class="close">×</a>
      <ul>
        <li>
          <%= value %>
        </li>
      </ul>
    </div>
  <% end %>

  <%= form_for :version, url: version_path(@version), method: :patch do |f| %>
  <% if @version.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@version.errors.count, "error") %> prohibited
      this version from being saved:
    </h2>
    <ul>
      <% @version.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

  <p align="center">
    <%= f.label :Version_Name %><br>
    <%= f.text_field :Version_Name %>
  </p>
  <p align="center">
    <%= f.check_box :active %> Active<br />
  </p>
  <br>
  <p align="center">
    <%= f.submit %>
  </p>

    <br>
    
    <p align="center"><%= link_to 'List of Versions for this system', system_path(@system) %></p>
<% end %>
</div>
</body>
